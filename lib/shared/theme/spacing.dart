import 'package:flutter/material.dart';

class SpacingTheme {
  /// Padding with 4px (spacing-inset-50)
  static const EdgeInsets inset50 = EdgeInsets.all(4);

  /// Padding with 8px (spacing-inset-100)
  static const EdgeInsets inset100 = EdgeInsets.all(8);

  /// Padding with 16px (spacing-inset-200)
  static const EdgeInsets inset200 = EdgeInsets.all(16);

  /// Padding with 20px (spacing-inset-300)
  static const EdgeInsets inset300 = EdgeInsets.all(20);

  /// Padding with 24px (spacing-inset-400)
  static const EdgeInsets inset400 = EdgeInsets.all(24);

  /// Padding with 32px (spacing-inset-500)
  static const EdgeInsets inset500 = EdgeInsets.all(32);

  /// Padding with 48px (spacing-inset-600)
  static const EdgeInsets inset600 = EdgeInsets.all(48);

  /// Padding with top/bottom: 8px left/right: 16px (spacing-squish-100)
  static const EdgeInsets squish100 =
      EdgeInsets.symmetric(vertical: 8, horizontal: 16);

  /// Padding with top/bottom: 12px left/right: 16px (spacing-squish-200)
  static const EdgeInsets squish200 =
      EdgeInsets.symmetric(vertical: 12, horizontal: 16);

  /// Padding with top/bottom: 16px left/right: 32px (spacing-squish-300)
  static const EdgeInsets squish300 =
      EdgeInsets.symmetric(vertical: 16, horizontal: 32);

  /// Padding with top/bottom: 32px left/right: 48px (spacing-squish-400)
  static const EdgeInsets squish400 =
      EdgeInsets.symmetric(vertical: 32, horizontal: 48);
}
